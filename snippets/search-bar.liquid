{% comment %}
<!-- /snippets/search-bar.liquid -->

A snippet to include a search bar anywhere in your theme.

To return only products in results:
- Manually add type=product to the search URL as a query parameter
- Or uncomment hidden input with value="product" below

{% endcomment %}

<form action="/search" method="get" class="search-bar" role="search">
    <input type="hidden" name="type" value="product">
    <span class="label">
        <span class="icon" aria-hidden="true"></span>
        <span class="fallback-text">{{ 'general.search.submit' | t }}</span>
    </span>
    <input class="search" type="search" name="q" value="{{ search.terms | escape }}" placeholder="{{ 'general.search.placeholder' | t }}" aria-label="{{ 'general.search.placeholder' | t }}">
    {% comment %}
    <button type="submit" class="button">
        <span class="icon" aria-hidden="true"></span>
        <span class="fallback-text">{{ 'general.search.submit' | t }}</span>
    </button>
    {% endcomment %}
</form>
