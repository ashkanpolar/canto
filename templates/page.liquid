<!-- /templates/page.liquid -->
<div class="page-container">
    <div class="section-header">
      <h1>{{ page.name }}</h1>
    </div>
    {% comment %}
      Regular page content goes here.
    {% endcomment %}
    <div class="rte">
      {{ page.content }}
    </div>
</div>
